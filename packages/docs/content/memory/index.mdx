import { Callout, Cards } from 'nextra/components'

# Memory

Memory gives Pandora long-term recall across conversations. It automatically remembers past interactions and lets the AI store facts, preferences, and knowledge.

## Quick start

```jsonc
"memory": {
  "type": "sqlite",
  "path": "data/memory.db",
  "embeddingModel": "openai/text-embedding-3-small"
}
```

Memory is optional. Without it, Pandora only sees messages from the current conversation.

## How it works

Pandora has two types of memory:

### Episodic memory (automatic)

Every interaction is automatically saved as an **episode**. Before responding, Pandora searches these episodes for relevant context.

```
User: "What's the weather in Tokyo?"
      ↓
[Search memory for: "weather Tokyo"]
      ↓
Found: [2 days ago] "User asked about weather. Prefers Celsius."
      ↓
Response includes context about past preference
```

Episodes are linked to conversations and cleaned up when you delete a conversation.

### Semantic memory (agent-controlled)

The AI can explicitly store **facts** — user preferences, knowledge, and instructions — using memory tools:

- `remember` — Store a fact with auto-deduplication
- `recall` — Search both episodic and semantic memories
- `getMemory` — Fetch full content of a memory record
- `forget` — Delete an outdated or incorrect fact

```
User: "I prefer dark mode for all apps"
      ↓
AI uses remember tool: "User prefers dark mode"
      ↓
Next conversation: AI recalls this preference
```

## Automatic behavior

The Gateway handles two automatic memory operations on every message, transparently and without the agent using any tools.

**Auto-recall** — Before passing a message to the agent, the Gateway searches memory for relevant context and injects brief summaries (truncated with IDs) of the most relevant facts and episodes into the system prompt. The agent can use `getMemory` to retrieve full details on demand. Episodes from the current conversation are excluded to avoid redundancy.

**Auto-episode** — After the agent finishes responding, the Gateway stores the interaction as an episodic memory. This runs in the background without blocking the response.

## Memory tools

When memory is configured, the agent has access to four tools:

### remember

Store a fact, preference, or piece of knowledge for future reference.

```
"Remember that I prefer TypeScript over JavaScript"
→ Stores: [user_preference] "User prefers TypeScript over JavaScript"
```

**Input:**
- `content` (string) — The fact, preference, or knowledge to remember
- `category` (optional) — One of `user_preference`, `knowledge`, `instruction` (defaults to `knowledge`)

**Categories:**
- `user_preference` — Personal preferences
- `knowledge` — Facts and information
- `instruction` — Directives and rules

Near-duplicate facts are automatically updated instead of creating duplicates, keeping memory clean over time.

### recall

Search memories for relevant information.

```
"What programming languages have we discussed?"
→ Searches episodes and facts for programming language mentions
```

**Input:**
- `query` (string) — What to search for in memory
- `type` (optional) — `all`, `episodes`, or `facts` (defaults to `all`)
- `limit` (optional) — Maximum results, 1-20 (defaults to 5)

Returns chunks with relevance scores from both episodic and semantic memories.

### getMemory

Fetch the full content of a memory record by its ID. Use when `recall` returns a chunk and you need the complete context.

**Input:**
- `id` (string) — The parent ID from recall search results
- `type` — `episode` or `fact`

### forget

Delete a stored fact by its ID. Episodic memories cannot be deleted directly — they are cleaned up automatically when conversations are deleted.

```
"Forget my preference for tabs over spaces"
→ Deletes the relevant fact
```

**Input:**
- `id` (string) — The parent ID of the fact to delete (from recall results)

## When to use memory

<Callout type="info">
**Use memory when** you want Pandora to remember preferences across sessions, have recurring topics or projects, or want context from past conversations.
</Callout>

<Callout type="warning">
**Skip memory when** you prefer fresh starts each conversation, are testing or developing, or privacy is a concern (memory persists data).
</Callout>

For implementation details (search algorithms, chunking strategy, deduplication thresholds), see [Memory Reference — Internals](/reference/memory-reference#memory-internals).

## Built-in backends

<Cards>
  <Cards.Card title="SQLite" href="/memory/sqlite" />
</Cards>

## Create your own

Want a different vector database? See [Custom Memory](/extensions/custom-memory) to build your own backend.
