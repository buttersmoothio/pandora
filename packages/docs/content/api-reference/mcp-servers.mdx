import { Tabs } from 'nextra/components'

# MCP Servers API

Endpoints for discovering MCP server state.

## GET /api/mcp-servers

Returns all configured MCP servers with their runtime state and loaded tools.

<Tabs items={['API', 'SDK']}>
  <Tabs.Tab>
```bash
curl http://localhost:4111/api/mcp-servers \
  -H "Authorization: Bearer <token>"
```
  </Tabs.Tab>
  <Tabs.Tab>
```ts
const { data } = await client.mcpServers.list()
```
  </Tabs.Tab>
</Tabs>

```json
{
  "data": [
    {
      "id": "github",
      "name": "GitHub",
      "type": "stdio",
      "enabled": true,
      "requireApproval": true,
      "tools": [
        { "id": "github_create_issue", "name": "Create Issue", "description": "..." },
        { "id": "github_list_repos", "name": "List Repos", "description": "..." }
      ],
      "error": null
    }
  ]
}
```

Each server object contains:

| Field | Type | Description |
|---|---|---|
| `id` | `string` | Server identifier (from config) |
| `name` | `string` | Display name |
| `type` | `'stdio' \| 'http'` | Transport type |
| `enabled` | `boolean` | Whether the server is enabled |
| `requireApproval` | `boolean` | Whether tool calls need user approval |
| `tools` | `{ id, name, description }[]` | Tools loaded from this server |
| `error` | `string?` | Connection or load error, if any |
| `authUrl` | `string?` | OAuth authorization URL when authorization is pending |

## Managing MCP Servers

MCP servers are managed through the [Configuration API](/api-reference/configuration). Add, update, or remove servers by patching the `mcpServers` key.

### Adding a server

<Tabs items={['API', 'SDK']}>
  <Tabs.Tab>
```bash
curl -X PATCH http://localhost:4111/api/config \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "mcpServers": {
      "github": {
        "command": "bunx",
        "args": ["@modelcontextprotocol/server-github"],
        "name": "GitHub",
        "env": ["GITHUB_PERSONAL_ACCESS_TOKEN"],
        "enabled": true,
        "requireApproval": true
      }
    }
  }'
```
  </Tabs.Tab>
  <Tabs.Tab>
```ts
await client.mcpServers.add('github', {
  command: 'bunx',
  args: ['@modelcontextprotocol/server-github'],
  name: 'GitHub',
  env: ['GITHUB_PERSONAL_ACCESS_TOKEN'],
  enabled: true,
  requireApproval: true,
})
```
  </Tabs.Tab>
</Tabs>

### Adding a remote server with headers

<Tabs items={['API', 'SDK']}>
  <Tabs.Tab>
```bash
curl -X PATCH http://localhost:4111/api/config \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "mcpServers": {
      "remote-tools": {
        "url": "https://example.com/mcp",
        "name": "Remote Tools",
        "headers": {
          "Authorization": "Bearer sk-abc123"
        },
        "enabled": true,
        "requireApproval": true
      }
    }
  }'
```
  </Tabs.Tab>
  <Tabs.Tab>
```ts
await client.mcpServers.add('remote-tools', {
  url: 'https://example.com/mcp',
  name: 'Remote Tools',
  headers: {
    Authorization: 'Bearer sk-abc123',
  },
  enabled: true,
  requireApproval: true,
})
```
  </Tabs.Tab>
</Tabs>

### Adding a remote server with OAuth

<Tabs items={['API', 'SDK']}>
  <Tabs.Tab>
```bash
curl -X PATCH http://localhost:4111/api/config \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "mcpServers": {
      "oauth-server": {
        "url": "https://example.com/mcp",
        "name": "OAuth Server",
        "oauth": true,
        "enabled": true,
        "requireApproval": true
      }
    }
  }'
```
  </Tabs.Tab>
  <Tabs.Tab>
```ts
await client.mcpServers.add('oauth-server', {
  url: 'https://example.com/mcp',
  name: 'OAuth Server',
  oauth: true,
  enabled: true,
  requireApproval: true,
})
```
  </Tabs.Tab>
</Tabs>

After adding, check `GET /api/mcp-servers` for the `authUrl` field. Direct the user to that URL to complete authorization.

### Disabling a server

<Tabs items={['API', 'SDK']}>
  <Tabs.Tab>
```bash
curl -X PATCH http://localhost:4111/api/config \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"mcpServers": {"github": {"enabled": false}}}'
```
  </Tabs.Tab>
  <Tabs.Tab>
```ts
await client.config.update({
  mcpServers: { github: { enabled: false } },
})
```
  </Tabs.Tab>
</Tabs>

### Removing a server

<Tabs items={['API', 'SDK']}>
  <Tabs.Tab>
```bash
curl -X PATCH http://localhost:4111/api/config \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"mcpServers": {"github": null}}'
```
  </Tabs.Tab>
  <Tabs.Tab>
```ts
await client.config.update({
  mcpServers: { github: null },
})
```
  </Tabs.Tab>
</Tabs>

### Server config fields

| Field | Required | Description |
|---|---|---|
| `command` | One of `command` or `url` | Command to execute for stdio transport |
| `args` | No | Arguments for the command |
| `url` | One of `command` or `url` | URL for HTTP transport |
| `enabled` | No | Whether the server is active (default: `true`) |
| `name` | No | Display name (defaults to server ID) |
| `env` | No | Environment variable names to forward from host to the server process |
| `requireApproval` | No | Whether tool calls need approval (default: `true`) |
| `headers` | No | Key-value pairs sent as HTTP headers to remote servers |
| `oauth` | No | Enable OAuth authentication for remote servers (default: `false`) |

## Types

These types are available from `@pandorakit/sdk/api`:

```ts
import type { McpServerInfo, McpToolOverview, AddMcpServerInput } from '@pandorakit/sdk/api'
```
