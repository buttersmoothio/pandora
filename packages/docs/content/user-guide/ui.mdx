# Chat UI

The official Pandora UI is a web app you can run alongside the server.

## Running

Start both the server and UI together from the workspace root:

```bash
bun run dev
```

Or separately:

```bash
# Terminal 1 — Server (port 4111)
cd packages/core && bun run dev

# Terminal 2 — UI (port 3000)
cd packages/ui && bun run dev
```

## Connecting to a Remote Server

By default, the UI connects to `http://localhost:4111`. To point it at a different server:

```bash
# .env.local
NEXT_PUBLIC_API_URL=https://your-pandora-server.example.com
```
