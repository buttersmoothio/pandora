import { Callout, Steps, Cards, Tabs } from 'nextra/components'

# Quickstart

Get Pandora running in 5 minutes.

## Prerequisites

- [Bun](https://bun.sh) installed (`curl -fsSL https://bun.sh/install | bash`)
- A [Vercel AI Gateway](https://vercel.com/ai-gateway) API key (gives access to 50+ AI models)
- For Telegram (optional): A bot token from [@BotFather](https://t.me/BotFather)

<Tabs items={['Web UI', 'Telegram']}>
<Tabs.Tab>

<Steps>

### Clone and install

```bash
git clone https://github.com/pandora-ai/pandora.git
cd pandora
bun install
```

### Create your config

Copy the example config:

```bash
cp config.example.jsonc config.jsonc
```

Edit `config.jsonc` with your keys:

```jsonc
{
  "$schema": "./config.schema.jsonc",
  "ai": {
    "gateway": {
      "apiKey": "YOUR_VERCEL_AI_GATEWAY_KEY"
    },
    "agents": {
      "operator": { "model": "google/gemini-3-flash" },
      "webSearchNative": { "model": "perplexity/sonar-pro" }
    }
  },
  "channels": {
    "web": {
      "port": 3000,
      "token": "pick-a-secret-token"
    }
  }
}
```

### Start Pandora

```bash
bun run dev
```

### Chat!

Open `http://localhost:3001` and enter your token. That's it!

</Steps>

</Tabs.Tab>
<Tabs.Tab>

<Steps>

### Clone and install

```bash
git clone https://github.com/pandora-ai/pandora.git
cd pandora
bun install
```

### Create your config

Copy the example config:

```bash
cp config.example.jsonc config.jsonc
```

Edit `config.jsonc` with your keys:

```jsonc
{
  "$schema": "./config.schema.jsonc",
  "ai": {
    "gateway": {
      "apiKey": "YOUR_VERCEL_AI_GATEWAY_KEY"
    },
    "agents": {
      "operator": { "model": "google/gemini-3-flash" },
      "webSearchNative": { "model": "perplexity/sonar-pro" }
    }
  },
  "channels": {
    "telegram": {
      "token": "YOUR_TELEGRAM_BOT_TOKEN",
      "ownerId": "YOUR_TELEGRAM_USER_ID"
    }
  }
}
```

<Callout type="info">
**Getting your Telegram user ID:** Message [@userinfobot](https://t.me/userinfobot) on Telegram.
</Callout>

### Start Pandora

```bash
bun run dev
```

You should see:

```
[Pandora] Starting...
[Channel] [telegram] Started (long polling mode)
```

### Chat!

Open Telegram and message your bot. That's it!

</Steps>

</Tabs.Tab>
</Tabs>

---

## Next steps

<Cards>
  <Cards.Card title="Configuration" href="/configuration" />
  <Cards.Card title="Agents" href="/agents" />
  <Cards.Card title="Web UI" href="/channels/web" />
  <Cards.Card title="Memory" href="/memory" />
  <Cards.Card title="Building Extensions" href="/extensions/overview" />
</Cards>
