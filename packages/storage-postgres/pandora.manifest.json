{
  "manifestVersion": 1,
  "id": "storage-postgres",
  "name": "PostgreSQL",
  "description": "PostgreSQL storage provider",
  "pandora": ">=0.0.1",
  "provides": {
    "storage": {
      "entry": "./src/index.ts",
      "sandbox": "host"
    }
  },
  "envVars": [{ "name": "DATABASE_URL" }]
}
